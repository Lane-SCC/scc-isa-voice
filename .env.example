# Example environment variables for the LFG server
# Copy this file to `.env` and fill the SMTP values to enable email alerts.

# Report recipient (you provided this address)
ALERT_EMAIL_TO=lane-sharpe@outlook.com

# SMTP settings (fill these out with your provider's values)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass

# Optional webhook/Slack fallbacks
SLACK_WEBHOOK_URL=
ALERT_WEBHOOK_URL=

# Admin API key (change to a secure random value)
ADMIN_API_KEY=change_this_to_secure_random
